// Widget card untuk satu kode booking di layar "Booking Saya".
// TODO Phase 5: implementasi setelah RecentBooking model tersedia.

import 'package:flutter/material.dart';

class BookingCodeCard extends StatelessWidget {
  const BookingCodeCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO Phase 5: tampilkan kode, nama cabang, tanggal slot, tombol lihat QR.
    return const Card(
      child: ListTile(title: Text('Booking Code Card (TODO Phase 5)')),
    );
  }
}
