# This directory holds dev-only JWT key material.
# Real PEM files are gitignored (see ../../.gitignore). This placeholder
# exists so the directory is present in a fresh clone; the README-prescribed
# `openssl` commands write jwt_private.pem and jwt_public.pem here.
